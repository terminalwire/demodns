<!DOCTYPE html>
<html>
<head>
  <title>EasyDNS</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <style>
    body { background-color: #f3f4f6; font-family: Arial, sans-serif; }
    nav { background: linear-gradient(to right, #1e40af, #3b82f6); padding: 1rem; color: white; display: flex; justify-content: space-between; align-items: center; }
    nav a { color: white; margin-left: 1rem; text-decoration: none; }
    .container { max-width: 40rem; margin: 2rem auto; background: white; padding: 1.5rem; border-radius: 8px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); }
    .form-group { margin-bottom: 1rem; }
    label { display: block; font-weight: bold; }
    input { width: 100%; padding: 0.5rem; border: 1px solid #ccc; border-radius: 4px; }
    .btn-group { display: flex; gap: 0.5rem; }
    .btn { padding: 0.5rem 1rem; border: none; border-radius: 4px; cursor: pointer; }
    .btn-cancel { background: #d1d5db; }
    .btn-submit { background: #3b82f6; color: white; }
  </style>
</head>
<body>
  <nav>
    <div class="text-xl font-bold">EasyDNS</div>
    <div>
      <a href="#">Dashboard</a>
      <a href="#">Domain Names</a>
      <a href="#">Certificates</a>
      <a href="#">Contacts</a>
      <a href="#">Templates</a>
    </div>
    <div>
      <a href="#">What's New</a>
      <a href="#">User Settings</a>
      <a href="#">Log out</a>
    </div>
  </nav>
  <div class="container">
    <h1>Register domain</h1>
    <%= form_with url: "/domains/register", method: "post" do %>
      <div class="form-group">
        <label>Domain Name</label>
        <%= text_field_tag :domain_name, nil %>
      </div>
      <p>Domain registrations are billed immediately upon successful registration. <a href="#">See our TLD price list</a> for prices.</p>
      <div class="btn-group">
        <%= button_tag "Cancel", type: "button", class: "btn btn-cancel" %>
        <%= submit_tag "Continue", class: "btn btn-submit" %>
      </div>
    <% end %>
  </div>
</body>
</html>
