<%= form_with(model: dns_record, url: (controller.action_name == "edit" ? @dns_record : domain_dns_records_path(@domain))) do |form| %>
  <% if dns_record.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dns_record.errors.count, "error") %> prohibited this DNS_record from being saved:</h2>

      <ul>
        <% dns_record.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :host, style: "display: block" %>
    <%= form.text_field :host %>
  </div>

  <div>
    <%= form.label :record_type, style: "display: block" %>
    <%= form.select :record_type, DnsRecord::RECORD_TYPES %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.label :ttl, style: "display: block" %>
    <%= form.number_field :ttl %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
